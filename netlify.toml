[build]
  command = "node -v && yarn -v && yarn run generate && ls -la .output && ls -la .output/public"
  publish = ".output/public"

[build.environment]
  NODE_VERSION = "18"
  NODE_OPTIONS = "--max-old-space-size=6144"
  NUXT_TELEMETRY_DISABLED = "1"